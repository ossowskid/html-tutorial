function getAverage(a, b,c,d,e,f) {
    
    const average = (a + b + c + d + e + f) / 6;
    console.log(average);
    return average;
}


const myResult = getAverage(7, 8, 9, 10, 11, 12);

console.log("the average is " + myResult);
